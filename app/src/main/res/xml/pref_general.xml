<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="temperature_list"
        android:title="@string/pref_title_unit_system"
        android:defaultValue="imperial"
        android:entries="@array/pref_unit_list"
        android:entryValues="@array/pref_example_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="days_list"
        android:title="@string/pref_title_days_list"
        android:defaultValue="7"
        android:entries="@array/pref_days_list"
        android:entryValues="@array/pref_days_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
